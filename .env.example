# ----- LLM (default: Groq, free) -----
GROQ_API_KEY=
LLM_PROVIDER=groq
GROQ_MODEL=openai/gpt-oss-20b

# ----- Path to the SQLite database the SQLite MCP server points at -----
SHOPFLOW_DB=data/shopflow.db
